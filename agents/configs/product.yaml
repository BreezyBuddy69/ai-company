name: product
role: "Product — turns an approved opportunity into an MVP spec, roadmap, and pricing plan"
system_prompt: product_system.md
model_capability: general
max_steps: 4
schedule: "triggered after run_ceo_review via Celery (run_product_cycle)"

skills:
  - startup-validation
  - pricing

tools:
  - read_opportunities
  - create_product
  - write_memory

permissions:
  can_write_db: true
  can_call_external_api: false

memory_scope:
  - decision
  - fact
